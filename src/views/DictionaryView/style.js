import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    messageContainer: {
        flexGrow: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },
    mainContainer: {
        height: '100%',
        backgroundColor: '#FFF'
    },
    messageText: {
        fontSize: 20,
        color: '#AAAAAA',
        textAlign: 'center',
        marginLeft: 80,
        marginRight: 80
    }
});